In this method we fist create a fifo which is nothing but a named file using the mkfifo() command. now we we write data into that fifo using the string array that we have created using the random string generator function which uses the help of rand function to generate random strings now we write this data along with the ids in the fifo meanwhile process 2 starts reading this fifo and prints the ids the highest id is then sent by process 2 using another fifo which is read in process 1 and printed this loop continues till the highest id 49 is reached.